import React from 'react';

// This component is no longer needed as About is now integrated into the Header
// Keeping this file for potential future use or as a backup

const About: React.FC = () => {
  return null;
};

export default About;